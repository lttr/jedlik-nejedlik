// Marker so Nuxt registers this directory as a layer. The bottom of the layer
// stack: generic pieces that every other layer and the root app may import.
export default defineNuxtConfig({
  components: false,
})
