// U+00A0. Czech never separates a number from its unit and never lets a line
// break inside a grouped number, so every number the shop renders joins its
// noun with this rather than a plain space.
export const NON_BREAKING_SPACE = " "
