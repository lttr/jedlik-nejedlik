/**
 * Hosts whose traffic must never reach analytics or ad tools. See
 * docs/analytics.md, „Ignored hosts".
 */
export const IGNORED_HOSTNAMES = ["localhost", "127.0.0.1", "jedlik-nejedlik-test.lttr.cz"]
