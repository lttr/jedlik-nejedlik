NUXT_PUBLIC_DIRECTUS_URL=https://example.com
# Seals the session cookie (nuxt-auth-utils). 32+ random characters; boot fails
# without it. In dev the module generates one into web/.env when unset.
NUXT_SESSION_PASSWORD=replace-with-32-plus-random-characters
